syntax="proto3";

//package =

message FullUrl {
  string val = 1;
}

message TinyUrl {
  string val = 1;
}

service TinyUrlServer{
  rpc Add(FullUrl) returns (TinyUrl) {}
  rpc Get(TinyUrl) returns (FullUrl) {}
}